/* ClassFlow — Production Configuration */
'use strict';

const CONFIG = {
    // Leave empty in development to auto-detect localhost/loopback.
    // Set to your production API URL (e.g. 'https://api.classflow.yourdomain.com') for release.
    API_URL: ''
};
